# ALGORITHM

```
TroubleSort(L) // L is a indexed List of Integers
    let done : = False
    while not done:
        done = true
        for i :=0;i<len(L)-2;i++
            if L[i]>L[i+2]
                done = false
```